
defaultNamespace: default

helm:
  chart: github.com/andeersg/helm_chart2
  # Any values that should be placed in the `values.yaml` and passed to helm during
  # install.
  valuesFiles:
    - rancher_values.yaml